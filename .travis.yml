language: python
sudo: false

node_js:
  - "7"

python:
  - "2.7"

install:
  - pip install yotta
  - npm install
  - npm run pxt:init
  - npm run pxt:install

cache:
  directories:
    - node_modules